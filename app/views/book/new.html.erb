<h1>画像投稿フォーム</h1>
  <%= form_with model: @post_book do |f| %>
    <h4>画像</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>名前</h4>
      <%= f.text_field :user_name %>
    <h4>説明</h4>
      <%= f.text_area :caption %>
    <%= f.submit '投稿' %>
  <% end %>